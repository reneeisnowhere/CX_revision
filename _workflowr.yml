# workflowr options
# Version 1.7.1

# The seed to use for random number generation. See ?set.seed for details.
seed: 20251031
# The working directory to build the R Markdown files. The path is relative to
# _workflowr.yml. See ?rmarkdown::render for details.
knit_root_dir: "."


# --- Site metadata ---
site:
  title: "CX revions"       # Replace with your project title
  author: "Renee Matthews"       # Replace with your name
  template: default               # Ensure the default template is used

# --- Git / GitHub settings ---
git:
  url: "https://github.com/reneem/CX_revision"  # Replace with your GitHub repo URL
